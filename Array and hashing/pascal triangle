Given row value and column value we need to find what element will be there? 
For that the brute force approach is to make  pascal triangle and find the element or 
use a shortcut if r=5 and c=3 so that the trick is r-1Cc-1 (combination) 
