class solution{
    public:
    bool ContainsDuplicate(vector<int>& nums){
        unordered_set<int> s;
        for(int i=0;i<nums.size();i++){
            if(s.find(nums)!=s.end()){
                return true;
            }
            s.insert(nums[i]);
        }
        return false;
    }
};
